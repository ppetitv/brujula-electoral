export class ChatManager {
    constructor() {
        this.chatLog = document.querySelector('.chat-log');
        this.setupEventListeners();
    }

    async startChatSequence(userQuery, isPreloaded = false) {
        // Lógica del chat...
    }

    async animateText(htmlContent, container) {
        // Lógica de animación...
    }
}